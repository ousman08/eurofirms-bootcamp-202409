import mongoose from 'mongoose'
import { User, Product, } from './models'

mongoose.connect('mongodb://127.0.0.1:27017/mattas')

 // .then(() => eliminamos la base de datos User y post.deleteMany

 // crear usuario y sus palometros 

 
 // administrador , 

 //return user.save()

 // user

 // product.of.admin con palometro foto, titulo, descripcion