console.log('TEST Array.prototype.some')

console.log('CASE find if exist furits with the letter M')

var fruits = ['Banana', 'Mango', 'Piña', 'Fresa', 'Manzana']

var exists = fruits.some(function(m) {
   return exists
})

console.log(exists)